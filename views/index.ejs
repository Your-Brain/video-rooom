<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Chat App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center h-screen">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg text-center">
        <h1 class="text-3xl font-bold text-white mb-4">Welcome to Video Chat</h1>

        <button onclick="createRoom()" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 mb-4">Create Room</button>

        <div>
            <input id="roomId" type="text" placeholder="Enter Room ID" class="px-4 py-2 rounded-lg border-none text-gray-900">
            <button onclick="joinRoom()" class="bg-green-500 text-white px-6 py-2 rounded-lg hover:bg-green-600">Join Room</button>
        </div>
    </div>

    <script>
        function createRoom() {
            window.location.href = '/room';
        }
        function joinRoom() {
            const roomId = document.getElementById('roomId').value;
            if(roomId) {
                window.location.href = `/room/${roomId}`;
            } else {
                alert('Please enter a valid Room ID.');
            }
        }
    </script>
</body>
</html>
