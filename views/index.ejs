<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join a Video Chat Room</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col items-center justify-center h-screen bg-gray-900 text-white">
    <h1 class="text-2xl font-bold mb-6">Welcome to Video Chat</h1>

    <a href="/new" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded-lg mb-4">
        Create a New Room
    </a>

    <div class="flex items-center gap-2">
        <input type="text" id="roomId" placeholder="Enter Room ID"
            class="p-2 rounded bg-gray-800 border border-gray-600 text-white focus:outline-none">
        <button onclick="joinRoom()" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg">
            Join Room
        </button>
    </div>

    <script>
        function joinRoom() {
            const roomId = document.getElementById("roomId").value;
            if (roomId.trim()) {
                window.location.href = `/${roomId}`;
            } else {
                alert("Please enter a valid room ID.");
            }
        }
    </script>
</body>
</html>
